[project]
name = "image-mcp"
version = "0.1.0"
description = "Image generation and editing via OpenRouter"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2.4",
    "openai>=1.0.0",
]
