[project]
name = "string-mcp"
version = "0.1.0"
description = "Text operations on files: head, tail, ripgrep"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2.4",
    "loguru>=0.7.3",
]
